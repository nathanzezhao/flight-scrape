# Postgres connection string (Vercel Postgres or Neon). Required in production
# for both the web/api layer and the scraper layer; omit locally to fall back
# to a local SQLite dev database.
DATABASE_URL=
